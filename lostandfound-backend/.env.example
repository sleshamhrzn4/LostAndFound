# Copy this file to .env and fill in your own values:
#   cp .env.example .env

# MongoDB Atlas example (replace user, password, and cluster):
MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/professionals

# The port the server listens on (optional — defaults to 3000).
PORT=3000

JWT_SECRET=your_jwt_secret_key

